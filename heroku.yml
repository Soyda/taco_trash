build:
  docker:
    web: frontend/Dockerfile
    worker: backend/Dockerfile
run: 
  web: streamlit run ./frontend/app.py
  worker: uvicorn ./backend/main:app --host 0.0.0.0 --port=${PORT:-5000}